name: hw-ct

on:
  push:
    branches:
      - main
      - 'feature/**'
  pull_request:
  workflow_dispatch:

jobs:
  hardware:
    uses: kaidokert/krabi-caliper-rs/.github/workflows/hw-ct.yml@hw-ct-v1
    with:
      invocation-name: fixed-bigint
      campaigns-json: '["fixed-bigint-jtrace-f407"]'
      working-directory: ct-verify/cyccnt-hardware
      prepared: true
